# node / vite
node_modules/
dist/
*.tsbuildinfo

# rust / tauri
src-tauri/target/
src-tauri/gen/

# python sidecar
sidecar/.venv/
__pycache__/
*.pyc

# playwright / test artifacts
.playwright-mcp/
fission-*.png
green-select.png

# python venv (any location)
.venv/

# os
.DS_Store

# scratch CAD artifacts: dev test parts, not shipped
src-tauri/*.sindri
src-tauri/*.step
src-tauri/*.stl
src-tauri/*.3mf

# research / experiment shelf: kept out of the repo
proving-ground/

# bundled Python sidecar runtime (built by scripts/build-sidecar-runtime.sh, ~800MB)
src-tauri/sidecar-runtime/

# secrets / local env: never commit
.env
.env.*
*.key
*.pem
*.p12
secrets.*
credentials.*

# editor / IDE
.vscode/
.idea/
*.swp
